---
import PostMeta from '~/components/PostMeta.astro'
import PostMetaRelative from '~/components/PostMetaRelative.astro'
import type { Post } from '~/types'

interface Props {
  post: Post
}

const { post } = Astro.props
---

<article class="prose">
  <PostMeta post={post} />
  <slot />
  <PostMetaRelative post={post} />
</article>
